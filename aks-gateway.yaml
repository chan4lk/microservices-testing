apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: emp-api
  namespace: beta
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: emp-api
          servicePort: 3333